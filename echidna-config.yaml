
# workers: 1
# workers: 2
workers: 4
symExec: true

testMode: assertion

prefix: echidna_
corpusDir: echidna-corpus

# testLimit: 100000000000 # infinite
# testLimit: 10000 # 10K
# testLimit: 100000 # 100K
testLimit: 1000000 # 1M
# testLimit: 10000000 # 10M
# testLimit: 100000000 # 100M

# shrinkLimit: 100000000000
# shrinkLimit: 100000 # 100K

# seqLen: 30
# seqLen: 250

maxTimeDelay: 100

# allContracts: true
# sender: ["0x10000", "0x20000", "0x30000", "0x40000"]
# filterFunctions: [
#     "ERC20PresetFixedSupply.transfer(address,uint256)",
#     "ERC20PresetFixedSupply.approve(address,uint256)",
#     "ERC20PresetFixedSupply.decreaseAllowance(address,uint256)",
#     "ERC20PresetFixedSupply.burn(uint256)",
#     "AddressDriver.give(uint256,address,uint128)",
#   ]
